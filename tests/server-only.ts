// Next.js intentionally makes `server-only` throw outside its compiler.
// Tests run exclusively in Node, so resolving the marker to this file keeps
// the production boundary intact while allowing server modules to be tested.
export {};
